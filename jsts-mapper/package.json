{
  "name": "jsts-mapper",
  "version": "1.0.0",
  "description": "Provides a command-line executable that will use a source map to convert back and forth between source and generated positions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Miles Rout <miles.rout@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "source-map": "^0.5.7",
    "yargs": "^8.0.2"
  }
}
